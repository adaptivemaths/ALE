version: 1
frontend:
  phases:
    preBuild:
      commands:
        - cd frontend
        - npm ci
    build:
      commands:
        - npm run build
  artifacts:
    files:
        - 'frontend/**/*'
    baseDirectory: frontend/build
  cache:
    paths:
        - frontend/node_modules/**/*
        